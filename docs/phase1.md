# Phase 1 Scope

Phase 1 implements only a single-node in-memory cache engine. It establishes the local storage, expiration, eviction, testing, and benchmarking foundation needed before distributed behavior is introduced.

Out of scope for this phase:

- networking
- replication
- clustering
- consensus
- sharding
- persistence
- async IO
